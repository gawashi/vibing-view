---
phase: 02-timeframes-free-tier-resilience
plan: 3
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - src/main/cache/CacheService.ts
  - src/renderer/components/Chart.tsx
  - tests/main/cache/CacheService.test.ts
autonomous: false
requirements: [CHART-03]
user_setup: []

must_haves:
  truths:
    - "Panning left into uncached history fetches only the missing left sub-range [range.from, oldestCached − 1 bar], not a full re-fetch (D-16 / DESIGN-ADDENDUM §1/§5)."
    - "Zooming and panning along the time axis works, and the freshly fetched older bars append seamlessly to the existing series."
    - "During a pan-triggered gap-fetch, existing cached bars stay rendered and only a subtle non-blocking indicator shows — no full-chart loading state re-triggers (UI-SPEC gap-fetch loading)."
    - "Pan events are debounced 300ms and a single-flight guard per (symbol, timeframe) prevents overlapping pans from double-fetching (DESIGN-ADDENDUM §5)."
  artifacts:
    - "src/main/cache/CacheService.ts — gap-fetch miss branch computing the narrowed left sub-range"
    - "src/renderer/components/Chart.tsx — subscribeVisibleLogicalRangeChange handler with debounce + single-flight + prefetch pad"
  key_links:
    - "Chart.tsx timeScale().subscribeVisibleLogicalRangeChange → debounced request → api.ohlcv.get(symbol, tf, {from, to}) with the narrowed gap range → CacheService fetches only that sub-range."
  prohibitions:
    - "MUST NOT re-fetch bars already covered by cache coverage — a pan fetches only the contiguous missing sub-range, never undefined/all (D-16)."
---

<objective>
Let a user pan and zoom through history without wasting FMP requests. Extend `CacheService`'s miss branch so a partially-covered range fetches only the missing left sub-range (contiguous with existing coverage, keeping the single-interval model), and subscribe to the chart's visible-range changes to trigger that gap-fetch — debounced, single-flight, with a one-viewport prefetch pad and a subtle non-blocking indicator.

Purpose: Third vertical slice — delivers CHART-03 (pan/zoom) and the API-saving invariant D-16. Depends on Plan 01 (Chart.tsx timeframe threading) and serializes after Plan 02 on `CacheService.ts` ownership.
Output: Smooth pan-left that lazily backfills only what's missing.

## Flagged Assumption (edge-probe unresolved for CHART-03)

CHART-03 came back unclassified from the edge probe (Japanese text). Its edge behavior is governed here by D-16 (fetch only the missing sub-range), DESIGN-ADDENDUM §1/§5 (contiguous single-interval, 300ms debounce, single-flight, one-viewport prefetch), and UI-SPEC E3 (subtle non-blocking indicator, cached bars stay rendered). No silent drop.

## Slice check

Before this plan the chart shows only the initially-fetched window per timeframe. After it, panning left loads older bars on demand — a new user-visible capability.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-timeframes-free-tier-resilience/02-DESIGN-ADDENDUM.md
@.planning/phases/02-timeframes-free-tier-resilience/02-PATTERNS.md
@.planning/phases/02-timeframes-free-tier-resilience/02-UI-SPEC.md
@src/main/cache/CacheService.ts
@src/renderer/components/Chart.tsx
@src/renderer/api.ts
@tests/main/cache/CacheService.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: CacheService fetches only the missing left sub-range</name>
  <files>src/main/cache/CacheService.ts, tests/main/cache/CacheService.test.ts</files>
  <read_first>
    - DESIGN-ADDENDUM §1 (coverage model: pan-left gap fetches [targetFrom, oldestCached − 1 bar], contiguous, no holes) and §5 (gap-fetch).
    - 02-PATTERNS.md section `src/main/cache/CacheService.ts` (the miss branch that currently fetches `undefined` = everything; `covers()` is the exact complement of the gap calc) and its test-analog note (fakeStore partial-coverage case).
    - src/main/cache/CacheService.ts (full file — `covers()` lines 10-17, `getOHLCV` lines 22-31; note Plan 02 already added the W/M short-circuit at the top — keep it, add gap logic to the intraday/daily miss branch below it).
    - tests/main/cache/CacheService.test.ts (`fakeStore` factory + provider spy).
  </read_first>
  <action>
    In src/main/cache/CacheService.ts, change the miss branch (the one that runs after the W/M short-circuit and the `covers()` hit check) so it distinguishes two cases: (a) no coverage at all (`cov === null`) → keep the existing "fetch initial" behavior (`provider.getOHLCV(symbol, tf, undefined)` for daily full-history / intraday initial span); (b) coverage exists but does not cover the requested `range` on the left (`range.from < cov.oldestTime`) → fetch ONLY the missing left sub-range `{ from: range.from, to: cov.oldestTime - 1 }` (one bar before the oldest cached, contiguous per §1), upsert (the existing `upsertBarsAndCoverage` extends the single interval leftward via its onConflict merge), then serve `store.getBars(symbol, tf, range)`. Do not fetch `undefined` when coverage already exists. Keep the change minimal and in the same file/style as `covers()`. Extend tests/main/cache/CacheService.test.ts: a fakeStore seeded with coverage `{oldestTime: t1, newestTime: t2}` and a request `range` starting at `t0 < t1` → assert `provider.getOHLCV` is called exactly once with a range whose `from === t0` and `to === t1 - 1` (NOT `undefined`).
  </action>
  <verify>
    <automated>cd /c/Users/010230240/work/trading-view && npm run typecheck && npx vitest run tests/main/cache/CacheService.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - With existing coverage `{oldestTime:t1,newestTime:t2}` and requested range `{from:t0,to:t2}` where `t0<t1`, `provider.getOHLCV` is called once with the third arg `{from:t0, to:t1-1}` — asserted by the spy, proving no already-cached bar is re-requested (D-16).
    - With `cov === null`, the miss branch still calls `provider.getOHLCV(symbol, tf, undefined)` (initial fetch unchanged).
    - The Plan 02 W/M short-circuit and the existing cache-hit path are untouched and their tests still pass.
  </acceptance_criteria>
  <done>CacheService backfills only the contiguous missing left sub-range on a partial-coverage request; the "narrowed range, not undefined" behavior is unit-asserted.</done>
</task>

<task type="auto">
  <name>Task 2: Chart subscribes to visible-range changes and triggers debounced single-flight gap-fetch</name>
  <files>src/renderer/components/Chart.tsx</files>
  <read_first>
    - DESIGN-ADDENDUM §5 (subscribeVisibleLogicalRangeChange, debounce 300ms, left-edge approach, prefetch pad = one visible-range width, single-flight per (symbol, tf)) and §9 (renderer wires visible-range change → gap-fetch IPC).
    - 02-PATTERNS.md section `src/renderer/components/Chart.tsx` (attach the subscription inside the existing create-once effect after addSeries; pair unsubscribe in cleanup like `chart.remove()`; the loading/error JSX absolute-overlay convention to reuse for the subtle indicator) and `## No Analog Found` (no transient-indicator precedent — follow UI-SPEC E3).
    - src/renderer/components/Chart.tsx (full file — the create-once effect lines 18-37, data-push effect lines 40-46, overlay JSX lines 48-60; note Plan 01 added the `timeframe` prop + qk.ohlcv(symbol, tf)).
    - src/renderer/api.ts (`api.ohlcv.get(symbol, tf, range)` already accepts a range; qk.ohlcv key).
  </read_first>
  <action>
    In src/renderer/components/Chart.tsx, inside the create-once chart effect (after `chart.addSeries(...)`), register `chart.timeScale().subscribeVisibleLogicalRangeChange(handler)` and unsubscribe it in the effect cleanup alongside `chart.remove()`. The handler: when the visible logical range's left edge approaches the start of the loaded data (e.g. `range.from` within ~one bar of index 0), compute a target time window extending left by one visible-range width (prefetch pad, §5), map the earliest currently-loaded bar time as the boundary, and request the missing older bars via `api.ohlcv.get(symbol, timeframe, { from: targetFrom, to: oldestLoadedTime })`. Debounce the handler 300ms (a `setTimeout` ref cleared on each fire — no new dependency). Guard with a single-flight `inFlightRef` keyed by `${symbol}:${timeframe}` so overlapping pans don't double-fetch; clear it when the request settles. On success, merge the returned older bars with current data and `setData` (do NOT call `fitContent()` here — that would jump the view; only append/prepend and let the user's pan position stand). Show a subtle non-blocking indicator while a gap-fetch is in flight using the existing absolute-overlay convention (e.g. a small "Loading history…" pill at the left edge / dimmed timescale strip) — never the full-chart `q.isLoading` overlay, and keep existing cached bars rendered throughout (UI-SPEC E3). Access the merged/older bars through the query client or a local ref; prefer reusing TanStack by calling `queryClient.fetchQuery`/`setQueryData` on `qk.ohlcv(symbol, timeframe)` so the cache stays the source of truth (mirror how SettingsDialog already uses queryClient). Mark the left-edge threshold and prefetch-pad width with a `// ponytail:` comment (tunable if pan feels jumpy).
  </action>
  <verify>
    <automated>cd /c/Users/010230240/work/trading-view && npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Chart.tsx registers `subscribeVisibleLogicalRangeChange` inside the create-once effect and calls the matching unsubscribe in that effect's cleanup (setup/teardown paired).
    - The gap-fetch handler calls `api.ohlcv.get(symbol, timeframe, range)` with a bounded `{from,to}` range (never `undefined`).
    - A debounce (300ms) and a single-flight guard keyed by symbol+timeframe are present in the source.
    - The success path does NOT call `timeScale().fitContent()` (view position is preserved during backfill).
    - `npm run build` succeeds (renderer bundles).
  </acceptance_criteria>
  <done>Panning left triggers a debounced, single-flight request for only the missing older bars, which append without resetting the view; a subtle indicator shows during the fetch. Typecheck + build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify pan backfills only the missing sub-range</name>
  <action>Checkpoint: halt after Tasks 1-2 pass. Present the verification steps below and wait for the resume signal before marking the plan complete.</action>
  <what-built>Visible-range subscription in the chart + narrowed-range gap-fetch in CacheService. Panning into uncached history now backfills lazily.</what-built>
  <how-to-verify>
    1. `npm run dev`, load a symbol on an intraday timeframe (e.g. 1h) so only the recent initial span is loaded.
    2. Open DevTools → Network. Pan the chart to the LEFT into older history.
    3. Confirm: exactly one FMP `historical-chart` request fires per pan-into-gap (after the 300ms debounce), and its `from`/`to` cover ONLY the older missing window — NOT the full history and NOT the already-loaded recent bars.
    4. Confirm the existing (recent) candles stay on screen throughout — no full-chart "Loading chart…" flash — and a subtle indicator appears briefly during the fetch.
    5. Pan rapidly back and forth: confirm you do not see a burst of duplicate overlapping requests (single-flight + debounce).
  </how-to-verify>
  <resume-signal>Type "approved" if pan backfills only the gap without a full refetch, or describe what over-fetched.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer pan events → main fetch | Renderer-computed range crosses to main; main still validates via zod on the FMP response before caching. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Denial of service | pan handler firing many FMP requests | medium | mitigate | 300ms debounce + single-flight guard per (symbol,tf) cap the request rate (§5); protects the free-tier budget. |
| T-02-07 | Tampering | narrowed-range fetch merging into cache | low | mitigate | Contiguous single-interval fetch (`[from, oldestCached−1]`) keeps coverage hole-free (§1); backfilled bars pass the same zod parse as any fetch before upsert. |
</threat_model>

<verification>
- `npx vitest run tests/main/cache/CacheService.test.ts` + typecheck + `npm run build` green.
- Manual (Task 3): one narrowed request per pan-into-gap, cached bars persist, no duplicate bursts.
</verification>

<success_criteria>
- ROADMAP SC2: a user can pan and zoom along the time axis, and panning into uncached history fetches only the missing sub-range, not a full re-fetch.
- D-16 enforced and unit-asserted (narrowed range, not undefined).
</success_criteria>

<output>
Create `.planning/phases/02-timeframes-free-tier-resilience/02-03-SUMMARY.md` when done.
</output>
