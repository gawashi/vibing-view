---
phase: 02-timeframes-free-tier-resilience
plan: 5
type: execute
wave: 3
depends_on: [02-01, 02-04]
files_modified:
  - src/renderer/components/ui/badge.tsx
  - src/renderer/components/ui/tooltip.tsx
  - src/renderer/components/ui/sonner.tsx
  - src/renderer/components/TimeframeRow.tsx
  - src/renderer/api.ts
  - src/renderer/App.tsx
  - src/renderer/components/SettingsDialog.tsx
autonomous: false
requirements: [DATA-04]
user_setup: []

must_haves:
  truths:
    - "Gated timeframe buttons are grayed out (muted text #8B92A0) and unselectable, each carrying a Lock (requires-plan) or Clock (rate-limited) badge whose tooltip states the distinct reason (D-13/D-14)."
    - "The requires-plan tooltip reads exactly 'Requires a paid FMP plan. Your current key doesn't support this timeframe.' and the rate-limited tooltip reads exactly 'Daily request limit reached for this timeframe. Resets tomorrow — cached data (if any) stays available.'"
    - "While the capability probe is in flight the row keeps last-known availability rendered (optimistic all-available on first-ever load), updating each button in place with no flash-then-demote; a failed probe retains last-known availability rather than blanking the row."
    - "When the currently-viewed timeframe hits a rate limit mid-session, a toast fires once reading 'Rate limit reached for {timeframe}. Showing cached data — new bars will load once the limit resets.' and cached bars stay visible — the chart does not blank or error (D-15)."
    - "Switching to a paid FMP key re-enables previously gated intraday timeframes with no code change — the row re-probes and the buttons re-enable (SC4)."
    - "The timeframe row always renders exactly 7 buttons in fixed order; availability varies but the row never changes shape (UI-SPEC zero-one-many / populated)."
    - { statement: "The gated-reason tooltip copy does not clip or overflow at the app shell's minimum window width.", verification: backstop }
  artifacts:
    - "src/renderer/components/ui/badge.tsx, tooltip.tsx, sonner.tsx — shadcn blocks"
    - "src/renderer/components/TimeframeRow.tsx — capability-gated buttons with Lock/Clock badge + tooltip"
    - "src/renderer/api.ts — qk.capabilities query key"
    - "src/renderer/App.tsx — <Toaster/> mount + mid-session rate-limit observer"
  key_links:
    - "TimeframeRow useQuery(qk.capabilities, api.capabilities.get) drives per-button disabled state; capabilities.get() from Plan 04 is the source."
    - "App-level observer reads the active (symbol,tf) ohlcv query state via the shared qk.ohlcv key (no extra fetch, no Chart.tsx edit) and fires the rate-limit toast."
    - "SettingsDialog invalidates ['capabilities'] on key set/clear so the row re-probes (SC4)."
  prohibitions:
    - "MUST NOT block or grey the row on an in-flight or failed probe — retain last-known availability (optimistic all-available before the first probe)."
---

<objective>
Deliver DATA-04's user-visible surface: gated timeframe buttons that are grayed, unselectable, and badged (Lock for requires-plan, Clock for rate-limited) with distinct tooltip copy; a "last-known + revalidate" probe UX with no flash-then-demote; a mid-session rate-limit toast that keeps cached bars on screen; and re-enable-on-paid-key. Consumes the capability engine and `capabilities.get()` from Plan 04.

Purpose: Final vertical slice — turns the invisible detection engine into the graceful-degradation UX a user actually sees. Completes ROADMAP SC3 and SC4.
Output: A timeframe row that visibly and correctly reflects what the current FMP key can do, and never shows a broken/empty chart for a gated reason.

## Slice check

Before this plan, gated timeframes fail silently/awkwardly. After it, the user sees exactly which timeframes their key supports and why the others are unavailable — and switching to a paid key lights them up.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-timeframes-free-tier-resilience/02-DESIGN-ADDENDUM.md
@.planning/phases/02-timeframes-free-tier-resilience/02-PATTERNS.md
@.planning/phases/02-timeframes-free-tier-resilience/02-UI-SPEC.md
@.planning/phases/02-timeframes-free-tier-resilience/02-CONTEXT.md
@src/renderer/components/TimeframeRow.tsx
@src/renderer/api.ts
@src/renderer/App.tsx
@src/renderer/components/SearchBar.tsx
@src/renderer/components/SettingsDialog.tsx
@src/renderer/components/ui/button.tsx
@src/renderer/components/ui/alert.tsx
@src/shared/ipc.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — sonner (+ shadcn radix deps)</name>
  <action>Checkpoint: halt before any install. Do NOT run `npx shadcn add badge tooltip sonner` until the human approves package legitimacy below; on approval the install proceeds in Task 2.</action>
  <what-built>Nothing yet — precedes `npx shadcn add badge tooltip sonner`. That install pulls the `sonner` npm package and Radix deps (`@radix-ui/react-tooltip`, `@radix-ui/react-toggle-group` if not already present). No RESEARCH.md Package Legitimacy Audit exists (research skipped), so per protocol these are [ASSUMED] and human-verified before install. sonner is the toast library referenced by shadcn's official Sonner block (UI-SPEC Registry Safety marks shadcn-official as gate "not required" for the registry, but the underlying npm package still gets a legitimacy confirmation).</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/sonner and https://ui.shadcn.com/docs/components/sonner
    2. Confirm `sonner` is the well-known toast library (by Emil Kowalski), actively maintained, correct name (not a typosquat).
    3. Confirm the shadcn Sonner block is the official one and note it imports `next-themes` — this app is NOT Next.js, so the generated sonner.tsx must be adapted to drop `next-themes` and hardcode the dark theme (see Task 2).
  </how-to-verify>
  <resume-signal>Type "approved" to allow the shadcn add in Task 2, or raise a concern / substitute.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install shadcn Badge/Tooltip/Sonner and gate the timeframe buttons</name>
  <files>src/renderer/components/ui/badge.tsx, src/renderer/components/ui/tooltip.tsx, src/renderer/components/ui/sonner.tsx, src/renderer/components/TimeframeRow.tsx, src/renderer/api.ts</files>
  <read_first>
    - UI-SPEC (whole file): the Copywriting Contract (exact tooltip + toast strings), Visual Hierarchy → Icon Usage (Lock/Clock are non-interactive status badges riding a labeled button; must carry a Tooltip), Color (gated = muted text #8B92A0 + icon, NOT destructive; active = accent #2E7DE1), and the `## UI Considerations` table (last-known+revalidate; gated/disabled; zero-one-many fixed 7).
    - DESIGN-ADDENDUM §8/§9 (CapabilityStatus values; capabilities.get()).
    - 02-PATTERNS.md sections `src/renderer/components/TimeframeRow.tsx` (SearchBar analog; disabled-prop precedent from SettingsDialog line 58; inline gated-badge sub-component guidance; optimistic all-available default when q.data is undefined), `ui/{...}.tsx` (button.tsx/alert.tsx cva/forwardRef/cn convention), and `## No Analog Found` (transient toast has no P1 precedent).
    - src/renderer/components/TimeframeRow.tsx (the Plan 01 version — the `// Plan 05: capability gating attaches here` marker); src/renderer/api.ts (qk.ohlcv from Plan 01 — add qk.capabilities alongside); src/shared/ipc.ts (CapabilityStatus, Api.capabilities.get); src/renderer/components/ui/button.tsx + alert.tsx (convention + lucide icon usage).
  </read_first>
  <action>
    Run `npx shadcn add badge tooltip sonner` (approved in Task 1) → generates src/renderer/components/ui/{badge,tooltip,sonner}.tsx under the components.json aliases. Adapt the generated sonner.tsx: remove the `next-themes` import/usage (this is not Next.js) and hardcode `theme="dark"` on the `<Sonner/>` (the app is dark-only per UI-SPEC) — mark with a `// ponytail: dark-only app, no next-themes` comment.

    In src/renderer/api.ts add `qk.capabilities: () => ['capabilities'] as const` (invalidatable by the same prefix SettingsDialog already uses for ohlcv).

    In src/renderer/components/TimeframeRow.tsx, add `const caps = useQuery({ queryKey: qk.capabilities(), queryFn: () => api.capabilities.get() })`. Compute per-button status from `caps.data` defaulting to all-`'available'` when `caps.data` is `undefined` (optimistic first-load; on `caps.isError` keep the last successful `caps.data` — TanStack retains it — so a failed probe does NOT blank the row). For each of the 7 items: if status is `'requires-plan'` or `'rate-limited'`, render the `ToggleGroupItem` with the native `disabled` prop (Radix passes it through — same idea as SettingsDialog's `disabled={key.length===0}`), muted text color (#8B92A0), and an inline badge sub-component: a `Lock` (requires-plan) or `Clock` (rate-limited) lucide icon wrapped in a shadcn `Tooltip` whose content is the EXACT copy — requires-plan: "Requires a paid FMP plan. Your current key doesn't support this timeframe."; rate-limited: "Daily request limit reached for this timeframe. Resets tomorrow — cached data (if any) stays available." The active/selected available button uses accent #2E7DE1 (existing token). Keep the fixed 7-item array (never data-driven). '1w'/'1M' are always available (derived). Do not add a "gated" color — express gating via opacity/muted + icon only (UI-SPEC Color).
  </action>
  <verify>
    <automated>cd /c/Users/010230240/work/trading-view && npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/renderer/components/ui/{badge,tooltip,sonner}.tsx` exist; sonner.tsx has NO `next-themes` import and sets a dark theme.
    - `qk.capabilities()` deep-equals `['capabilities']`.
    - In TimeframeRow, a timeframe whose status is 'requires-plan' or 'rate-limited' renders its ToggleGroupItem with `disabled` set (unselectable) plus a Lock/Clock icon; an available one is selectable.
    - When `caps.data` is undefined, all buttons are treated available (optimistic) — no button is disabled during the first in-flight probe.
    - The two gated tooltip strings appear verbatim in the source (exact copywriting-contract match).
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>The timeframe row gates unavailable timeframes (disabled + Lock/Clock + distinct tooltip), stays optimistic during/after a probe, and never adds a gated color. Typecheck + build green.</done>
</task>

<task type="auto">
  <name>Task 3: Toaster mount, mid-session rate-limit toast, and re-probe on key change</name>
  <files>src/renderer/App.tsx, src/renderer/components/SettingsDialog.tsx</files>
  <read_first>
    - UI-SPEC Copywriting Contract (mid-session toast string) + `## UI Considerations` error(mid-session) row (toast fires once, cached bars remain, chart does not blank — D-15).
    - 02-PATTERNS.md sections `src/renderer/App.tsx` (mount `<Toaster/>` once at root, sibling to the top-level div — NOT inside Chart; D-06/D-07 restore effect unaffected) and `## No Analog Found` (transient toast — follow UI-SPEC copy + DESIGN-ADDENDUM transient-vs-sticky distinction).
    - src/renderer/App.tsx (full file — activeSymbol from store, `timeframe` useState from Plan 01, the `<main>` region).
    - src/renderer/components/SettingsDialog.tsx (lines 19-31 — save()/clear() already call `queryClient.invalidateQueries({ queryKey: ['ohlcv'] })`; add the capabilities invalidation there).
    - src/renderer/api.ts (qk.ohlcv, qk.capabilities); src/renderer/components/ui/sonner.tsx (the `toast` export).
  </read_first>
  <action>
    In src/renderer/App.tsx: mount `<Toaster />` (from ui/sonner) once as a sibling of the root `<div>`. Add a small rate-limit observer WITHOUT editing Chart.tsx: read the active `symbol` (store) and `timeframe` (existing App useState), and observe the SAME ohlcv query the chart uses via `useQuery({ queryKey: qk.ohlcv(symbol, timeframe), queryFn: () => api.ohlcv.get(symbol, timeframe, undefined), enabled: !!symbol })` — because the queryKey matches Chart's, TanStack shares the one cache entry (no extra fetch). In a `useEffect` keyed on that query's error/status: if the failure classifies as a rate limit (the typed status/body error surfaced from Plan 04, or the capabilities map showing the active tf as 'rate-limited'), call `toast(...)` once with the exact copy "Rate limit reached for {timeframe}. Showing cached data — new bars will load once the limit resets." (substitute the button label for {timeframe}), and invalidate `['capabilities']` so the row re-gates. Do NOT clear or replace the chart's data — cached bars stay (TanStack keeps previous data; the chart keeps rendering). Guard so the toast fires once per rate-limit transition, not on every render.

    In src/renderer/components/SettingsDialog.tsx: in both `save()` and `clear()`, add `queryClient.invalidateQueries({ queryKey: ['capabilities'] })` next to the existing `['ohlcv']` invalidation, so switching to a paid key (or clearing) re-probes and re-enables intraday buttons with no code change (SC4).
  </action>
  <verify>
    <automated>cd /c/Users/010230240/work/trading-view && npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `<Toaster/>` is mounted exactly once at the App root (not inside Chart).
    - The rate-limit observer uses `qk.ohlcv(symbol, timeframe)` (the same key Chart uses) so no additional network request is introduced (shared cache entry).
    - The mid-session toast string appears verbatim and fires once per rate-limit transition (guarded), and no code path clears the chart's series data on rate limit.
    - SettingsDialog `save()` and `clear()` both invalidate `['capabilities']` (in addition to `['ohlcv']`).
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>A single root Toaster shows the mid-session rate-limit toast once (cached bars preserved), and key changes re-probe capabilities so a paid key re-enables intraday — all without touching Chart.tsx.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify graceful degradation UX end-to-end</name>
  <action>Checkpoint: halt after Tasks 2-3 pass. Present the verification steps below and wait for the resume signal before marking the plan complete.</action>
  <what-built>Capability-gated timeframe buttons (Lock/Clock + tooltip), last-known+revalidate probe UX, mid-session rate-limit toast, and re-probe on key change.</what-built>
  <how-to-verify>
    1. `npm run dev` with a FREE-tier FMP key. Load a symbol. Confirm intraday timeframes that the free tier can't serve render grayed with a Lock badge; hovering shows "Requires a paid FMP plan…". D/W/M and any served intraday stay active. Clicking a gated button does nothing (unselectable).
    2. Confirm the requires-plan vs rate-limited tooltips read exactly as specified and use Lock vs Clock icons respectively (not the same icon, not a destructive/red color).
    3. On first load, confirm buttons don't flash-then-demote — they update in place as the probe resolves; the row never goes blank.
    4. (If reproducible) Exhaust the daily budget while viewing an active timeframe → confirm a single toast "Rate limit reached for {tf}…" appears and the existing candles stay on screen (no blank/error chart).
    5. Enter a PAID key in Settings → confirm previously-locked intraday buttons re-enable without restarting the app (re-probe on key change).
    6. Narrow the window to its minimum width and re-hover a gated badge → confirm the tooltip copy is not clipped/overflowing (backstop check).
  </how-to-verify>
  <resume-signal>Type "approved" if degradation is graceful per the UI-SPEC, or describe what looked wrong (include the window width for step 6 if the tooltip clipped).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main → renderer (capabilities.get) | Renderer consumes only the classified enum map to drive button state; no raw FMP data. |
| renderer state → UI | A stale/failed probe must degrade to last-known, never to a blank or all-locked row. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-12 | Denial of service (self-inflicted UX) | probe in-flight/failure blanking the row | medium | mitigate | Optimistic all-available default + retain last-known on error (UI-SPEC last-known+revalidate) — a failed probe never disables the whole row. |
| T-02-13 | Information disclosure | tooltip / toast copy | low | accept | Copy states plan/rate-limit status only; exposes no key material or endpoint detail. Rationale: this is the intended user-facing signal. |
| T-02-SC | Tampering | `npx shadcn add ... sonner` (npm install) | high | mitigate | Blocking-human legitimacy checkpoint (Task 1) before install; sonner is the official shadcn Sonner dependency, verified via npmjs.com. |
</threat_model>

<verification>
- `npm run typecheck` + `npm run build` green.
- Manual (Task 4): gated buttons with correct Lock/Clock + exact tooltip copy, no flash-then-demote, mid-session toast once with cached bars preserved, paid-key re-enable, tooltip no-clip at min width (backstop).
- Renderer component test harness is intentionally NOT introduced (P1 established none; jsdom/testing-library would be a cross-cutting infra addition out of scope for this UI slice). Typecheck + build are the automated floor; the human-verify checkpoint is the behavioral acceptance gate — a deliberate call consistent with P1, not a silent gap.
</verification>

<success_criteria>
- ROADMAP SC3 (completed): unavailable/rate-limited timeframes show a distinct "requires higher plan" / "rate-limited" state rather than an empty or broken chart.
- ROADMAP SC4 (completed): switching to a paid key re-enables gated intraday with no code change (capability re-probed on key change).
- D-13, D-14, D-15 satisfied; all UI-SPEC `## UI Considerations` covered items + the backstop are represented and verified.
</success_criteria>

<output>
Create `.planning/phases/02-timeframes-free-tier-resilience/02-05-SUMMARY.md` when done.
</output>
