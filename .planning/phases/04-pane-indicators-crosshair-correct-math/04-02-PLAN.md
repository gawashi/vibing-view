---
phase: 04-pane-indicators-crosshair-correct-math
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/renderer/indicators/volume.ts
  - src/renderer/indicators/registry.ts
  - src/renderer/store.ts
  - src/renderer/components/AddIndicatorMenu.tsx
  - tests/indicators/volume.test.ts
autonomous: true
requirements: [IND-04]
must_haves:
  truths:
    - "Volume is always present as a fixed sub-pane directly below the price pane once a symbol has bars; it is never zero-state, never toggled off, and not addable/removable via the menu (IND-04, D-34, UI E1 populated)."
    - "Volume bars are colored by candle direction: close≥open → #22C55E (green), else #EF4444 (red), matching the candlestick up/down colors exactly (D-43)."
    - "Volume's pane legend shows only label + cursor value (raw volume, thousands-separated e.g. 12,450,200) with no eye/gear/× action icons (D-34, UI-SPEC copywriting)."
    - "'Volume' does not appear in the '+指標' menu — it is not a user-addable instance (D-34, Landmine #8)."
    - "The Volume pane's crosshair readout and per-pane legend work with no Volume-specific code — reused from the Plan 01 generic machinery (CHART-04)."
  artifacts:
    - src/renderer/indicators/volume.ts
    - src/renderer/indicators/registry.ts
    - src/renderer/store.ts
    - src/renderer/components/AddIndicatorMenu.tsx
    - tests/indicators/volume.test.ts
  key_links:
    - "volume is a normal registered module (pane:'separate', one histogram output, per-bar color from close≥open) reusing Plan 01's reconcile + legend + crosshair machinery — DESIGN §3 (no Volume-specific rendering path)."
    - "one `fixed: true` seeded instance drives all three touch points: store seed (always present), removeIndicator guard (cannot remove), AddIndicatorMenu filter (cannot add)."
  prohibitions:
    - "Do NOT add a Volume-specific rendering branch to Chart.tsx — volume reuses the generic separate-pane histogram machinery from Plan 01 (DESIGN §3). This plan does not modify Chart.tsx."
    - "removeIndicator MUST ignore fixed instances (guard e.g. `i.id !== id || i.fixed`) so Volume can never be removed (D-34)."
    - "Volume must NOT be listed in the '+指標' menu — AddIndicatorMenu must filter it out (Landmine #8)."
---

<objective>
Add Volume as an always-on fixed sub-pane (D-34) by registering it as a normal indicator module and seeding one `fixed: true` instance — reusing the entire Plan 01 pipeline (reconcile, per-pane legend, crosshair) with a single flag and one menu filter. No Volume-only code path.

Purpose: prove the generic machinery from Plan 01 absorbs a histogram, fixed-pane indicator with only a module + a flag (IND-01 pluggability), and deliver IND-04.
Output: a green/red volume histogram pane below price, present for every symbol, excluded from the add-menu and non-removable.
</objective>

<artifacts_this_plan_produces>
- `volume` indicator module (`pane: 'separate'`, `outputs: [{ key: 'volume', kind: 'histogram' }]`, per-bar color, `params: []`) — `volume.ts`
- `volume` registered in `registry.ts`
- A `fixed: true` volume instance seeded into the initial store `indicators` array; `removeIndicator` ignores fixed instances — `store.ts`
- `volume` exclusion filter in the '+指標' menu — `AddIndicatorMenu.tsx`
- `tests/indicators/volume.test.ts`
</artifacts_this_plan_produces>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/phases/04-pane-indicators-crosshair-correct-math/04-DESIGN.md
@.planning/phases/04-pane-indicators-crosshair-correct-math/04-PATTERNS.md
@.planning/phases/04-pane-indicators-crosshair-correct-math/04-UI-SPEC.md
@.planning/phases/04-pane-indicators-crosshair-correct-math/04-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Volume indicator module + registration</name>
  <files>src/renderer/indicators/volume.ts, src/renderer/indicators/registry.ts</files>
  <read_first>
    - PATTERNS.md `volume.ts` section (ma.ts skeleton shape, DESIGN §5 "出来高は math 不要") and `registry.ts` section (one-line addition)
    - src/renderer/indicators/ma.ts (module skeleton), src/renderer/indicators/types.ts (the `HistPoint` + `pane`/histogram fields added in Plan 01), src/renderer/indicators/registry.ts (current `{ ma, bb, rsi }`)
    - src/renderer/components/Chart.tsx candlestick options (upColor `#22C55E` / downColor `#EF4444`) — reuse the exact same pair (D-43)
    - src/shared/types.ts (`Bar` has `volume`, `open`, `close`)
  </read_first>
  <action>Create volume.ts as an IndicatorModule: `type: 'volume'`, `label: () => 'Volume'`, `pane: 'separate'`, `params: []`, `defaults: {}`, `outputs: [{ key: 'volume', kind: 'histogram' }]`. `compute(bars)` maps each bar to a `HistPoint` `{ time, value: b.volume, color: b.close >= b.open ? '#22C55E' : '#EF4444' }` (D-43) — no math.ts call (DESIGN §5). Register `volume` in registry.ts (add import + extend the literal). Do NOT touch Chart.tsx — the Plan 01 histogram branch already renders it.</action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0 and `npm test` stays green.
    - `src/renderer/indicators/registry.ts` includes `volume`.
    - `src/renderer/indicators/volume.ts` sets `pane: 'separate'`, a single `kind: 'histogram'` output, and colors each point `#22C55E`/`#EF4444` by `close >= open`.
  </acceptance_criteria>
  <done>Volume is a registered histogram module that colors bars by candle direction, computed directly from bars with no math dependency.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Seed fixed Volume instance, guard removal, exclude from menu</name>
  <files>src/renderer/store.ts, src/renderer/components/AddIndicatorMenu.tsx</files>
  <read_first>
    - PATTERNS.md `store.ts` section (addIndicator analog for the seed shape; removeIndicator guard) and `AddIndicatorMenu.tsx` section (unfiltered `Object.values(registry).map` at ~21-32, Landmine #8)
    - src/renderer/store.ts (current initial `indicators: []`, `addIndicator`, `removeIndicator`; and the `crosshair` slice added in Plan 01), src/renderer/indicators/types.ts (`fixed?` flag added in Plan 01)
    - src/renderer/components/AddIndicatorMenu.tsx (current file)
    - DESIGN §3 (fixed volume seed + removeIndicator ignores fixed)
  </read_first>
  <action>In store.ts, seed the initial `indicators` array with one Volume instance built the same shape `addIndicator` produces (`id`, `type: 'volume'`, `params: {}`, `colors: {}` placeholder — colors are per-bar in compute, not palette-assigned, D-43), plus `fixed: true`. Use the module-level `nextId` counter for its id so ids stay JSON-stable. Guard `removeIndicator` to ignore fixed instances (keep an instance if `i.id !== id || i.fixed`) per DESIGN §3. In AddIndicatorMenu.tsx add a filter excluding `volume` before the `.map` (e.g. `Object.values(registry).filter((m) => m.type !== 'volume')`) — the smallest diff per Landmine #8 (YAGNI: D-34 only ever applies to volume, no generic hideFromMenu field needed).</action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run build</automated>
    <human-check>Launch `npm run dev`, select a symbol: a green/red Volume histogram pane appears directly below the price pane, its legend shows only "Volume" + a cursor value (no eye/gear/× icons); open '+指標' — RSI and MACD are listed but Volume is NOT; there is no way to remove the Volume pane.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0 and `npm run build` exits 0.
    - `src/renderer/store.ts` initial `indicators` contains one instance with `type: 'volume'` and `fixed: true`; `removeIndicator` preserves fixed instances.
    - `src/renderer/components/AddIndicatorMenu.tsx` filters out `volume` (does not render a Volume item).
    - Human-check confirms the Volume pane is present, icon-free in its legend, absent from the menu, and non-removable.
  </acceptance_criteria>
  <done>A fixed Volume pane is always present, its legend has no action icons, it cannot be added from the menu, and it cannot be removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Volume compute unit test</name>
  <files>tests/indicators/volume.test.ts</files>
  <read_first>
    - tests/indicators/math.test.ts (Vitest import/describe/it + comment-walkthrough house style)
    - src/renderer/indicators/volume.ts (subject), src/shared/types.ts (`Bar` shape for building fixtures)
    - VALIDATION.md Per-Task map (IND-04 unit row: color-by-direction)
  </read_first>
  <behavior>
    - `volume.compute(bars)` returns `{ volume: HistPoint[] }` with one point per bar, `value` = bar volume.
    - A bar with `close >= open` gets color `#22C55E`; a bar with `close < open` gets color `#EF4444` (D-43).
    - The `close === open` boundary is green (`close >= open`).
  </behavior>
  <action>Create tests/indicators/volume.test.ts mirroring math.test.ts conventions. Build a small `Bar[]` with mixed up/down/equal candles and assert the volume module's compute output length, per-point `value`, and per-point `color` by direction, including the `close === open` boundary.</action>
  <verify>
    <automated>npx vitest run tests/indicators/volume.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/indicators/volume.test.ts` exits 0.
    - The test asserts green for `close >= open` (including equality) and red for `close < open`.
  </acceptance_criteria>
  <done>Volume's direction-based coloring logic is covered by a fast unit test that passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Volume has NO user-editable parameters (`params: []`); its bars and colors are computed from already-cached OHLCV bars (D-26). No user input crosses into this plan, no network/IPC/file/auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| — | — | — | — | n/a | No applicable STRIDE threat: Volume takes no user input (no V5 surface), performs no I/O, and installs no packages. No new trust boundary (RESEARCH Security Domain). Nothing blocks under `security_block_on: high`. |
</threat_model>

<verification>
- `npm run typecheck` exits 0; `npm run build` exits 0; `npx vitest run tests/indicators/volume.test.ts` exits 0; `npm test` stays green.
- Manual UAT confirms the Volume pane renders below price with direction-colored bars, an icon-free legend, menu exclusion, and non-removability — reusing Plan 01's machinery with no Chart.tsx change.
</verification>

<success_criteria>
- IND-04 (Volume in a dedicated pane) met: an always-on green/red volume histogram pane, colored by candle direction, present for every symbol.
- Proof of IND-01 pluggability: a new fixed histogram indicator is added with only a module + a flag + a menu filter, zero Chart.tsx changes.
</success_criteria>

<output>
Create `.planning/phases/04-pane-indicators-crosshair-correct-math/04-02-SUMMARY.md` when done.
</output>
