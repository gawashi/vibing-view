---
phase: 04-pane-indicators-crosshair-correct-math
plan: 03
type: execute
wave: 3
depends_on: [04-01]
files_modified:
  - src/renderer/indicators/macd.ts
  - src/renderer/indicators/math.ts
  - src/renderer/indicators/registry.ts
  - src/renderer/store.ts
  - tests/indicators/macd.test.ts
autonomous: true
requirements: [IND-06]
must_haves:
  truths:
    - "User can add MACD via the '+指標' menu and it renders in its own sub-pane with a MACD line, a signal line in a distinguishable second palette hue, and a histogram, defaults 12/26/9 (IND-06, D-44/47)."
    - "The MACD histogram uses 4 colors by sign × rising/falling relative to the previous bar (dark/light green for positive, dark/light red for negative), decided per-bar inside compute — not in Chart.tsx (D-42)."
    - "The MACD pane shows a zero guide line; the MACD scale itself auto-scales (D-46)."
    - "MACD's signal line is the EMA of the MACD line (not SMA), and histogram = macdLine − signal; all three outputs are bar-aligned (D-50, IND-06)."
    - "MACD's crosshair readout in its pane legend shows three values in fixed order 'MACD {v} Signal {v} Hist {v}' at 2 decimals, reusing Plan 01's crosshair machinery (CHART-04, UI E3)."
    - statement: "MACD readout ('MACD {v} Signal {v} Hist {v}') at 2-decimal precision with large price-scale symbols (4+ integer digits) must not wrap or clip within the fixed-height legend row"
      verification: backstop
  artifacts:
    - src/renderer/indicators/macd.ts
    - src/renderer/indicators/math.ts
    - src/renderer/indicators/registry.ts
    - src/renderer/store.ts
    - tests/indicators/macd.test.ts
  key_links:
    - "macd() reuses the existing ema() via a compact-defined-suffix → ema → re-expand step (Landmine #5) — it never passes the gappy bar-aligned macdLine into ema() directly."
    - "macd is a normal registered module (pane:'separate', 2 line outputs + 1 histogram output) reusing Plan 01's reconcile/legend/crosshair machinery; only the palette seed in store distinguishes its two lines (D-47/UI-SPEC)."
  prohibitions:
    - "Do NOT pass the bar-aligned macdLine (which has a leading gap of `slow-1`) directly into ema() — ema() tolerates a leading gap only on an otherwise-dense array; compact the contiguous defined suffix into a number[], run ema(), then re-expand offset by the stripped leading count (Landmine #5)."
    - "4-color histogram logic lives in macd.ts compute (per-bar, comparing to zero and the previous bar), NOT in Chart.tsx (DESIGN §2: per-bar color は module.compute が返す)."
    - "Do NOT regress MA/RSI/BB colors — the consecutive-palette seed applies ONLY to multi-line modules that have no band output (BB keeps its single-color look)."
    - "This plan must NOT modify Chart.tsx — MACD reuses the generic line + histogram + guide machinery from Plan 01."
---

<objective>
Add MACD as a sub-pane indicator (line + signal + 4-color histogram + zero line, default 12/26/9) by adding the `macd()` math and a `macd` module that reuse Plan 01's generic machinery — proving the pipeline absorbs a multi-output, multi-color, auto-scaled indicator with zero Chart.tsx changes. Encodes the two trickiest math landmines: the signal-line gap-compaction (Landmine #5) and per-bar 4-color histogram (D-42).

Purpose: deliver IND-06 and complete the three sub-pane indicators; the only nuance beyond "register a module" is the signal-EMA gap-compaction and giving the two MACD lines distinguishable hues.
Output: a MACD pane with a zero line, a 4-color histogram, and two distinctly-colored lines, with a 3-value synchronized crosshair readout.
</objective>

<artifacts_this_plan_produces>
- `macd(values, fast, slow, signal)` function (returns bar-aligned macdLine, signal, histogram; signal via compact→ema→re-expand) — `math.ts`
- `macd` indicator module (`pane: 'separate'`, outputs `macd`/`signal` lines + `histogram`, static zero-line `guides`, per-bar 4-color histogram in compute) — `macd.ts`
- `macd` registered in `registry.ts`
- Consecutive-palette color seed for multi-line modules without a band output (gives macd/signal two hues), keyed off output structure not indicator type — `store.ts`
- `tests/indicators/macd.test.ts`
</artifacts_this_plan_produces>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/phases/04-pane-indicators-crosshair-correct-math/04-DESIGN.md
@.planning/phases/04-pane-indicators-crosshair-correct-math/04-RESEARCH.md
@.planning/phases/04-pane-indicators-crosshair-correct-math/04-PATTERNS.md
@.planning/phases/04-pane-indicators-crosshair-correct-math/04-UI-SPEC.md
@.planning/phases/04-pane-indicators-crosshair-correct-math/04-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MACD math + module + registration + two-hue palette seed</name>
  <files>src/renderer/indicators/math.ts, src/renderer/indicators/macd.ts, src/renderer/indicators/registry.ts, src/renderer/store.ts</files>
  <read_first>
    - RESEARCH Q7 (MACD = EMA signal; the exact compact/re-expand mechanics for reusing ema(); minimum-fixture-length note) and Landmine #5
    - PATTERNS.md `macd.ts` section (bb.ts multi-output toLineData helper + new toHistData helper; outputs shape; static zero-line guide), `math.ts` section (ema() analog + Landmine #5), `store.ts` section (addIndicator palette assignment)
    - src/renderer/indicators/math.ts (existing ema/sma/bollinger, ZERO-imports contract), src/renderer/indicators/bb.ts (multi-output construction), src/renderer/indicators/types.ts (HistPoint + histogram/guides from Plan 01), src/renderer/indicators/registry.ts, src/renderer/store.ts (addIndicator, and the volume seed + crosshair slice from Plans 01/02)
    - DESIGN §5 (macd formula) and §1 (guides), UI-SPEC Color table (MACD 4 histogram shades; two adjacent line hues)
  </read_first>
  <behavior>
    - `macd(values, fast, slow, signal)` returns `{ macd, signal, histogram }` each an `Array<number|undefined>` bar-aligned: macdLine = ema(values,fast) − ema(values,slow) (defined from index slow−1); signal = EMA of macdLine defined from index slow−1+(signal−1); histogram = macdLine − signal where both defined.
    - Signal is computed by compacting macdLine's contiguous defined suffix to a dense number[], running ema(compacted, signal), then re-expanding offset by the stripped leading count (Landmine #5) — never ema(macdLine) directly.
    - `macd` module compute returns `{ macd: LineData[], signal: LineData[], histogram: HistPoint[] }`; each histogram point's color is one of 4 by sign×(rising/falling vs previous histogram value) per D-42.
    - Adding a `macd` instance seeds `colors.macd` and `colors.signal` to two different palette entries; adding an `ma`/`rsi` (single line) or `bb` (band) instance is unchanged.
  </behavior>
  <action>Add `macd(values, fast, slow, signal)` to math.ts preserving the ZERO-imports contract: compute macdLine = elementwise ema(values,fast) − ema(values,slow) (undefined where either is undefined), then the signal via the compact→ema→re-expand step (Landmine #5 — slice the contiguous defined suffix of macdLine into a dense number[], call the existing `ema(compacted, signal)`, re-expand offsetting by the number of leading undefineds removed), then histogram = macdLine − signal. Create macd.ts as an IndicatorModule copying bb.ts's multi-output shape: `type: 'macd'`, `label: (p) => \`MACD ${p.fast},${p.slow},${p.signal}\``, `pane: 'separate'`, `defaults: { fast: 12, slow: 26, signal: 9 }`, number params for fast/slow/signal + color, `outputs: [{ key: 'macd', kind: 'line' }, { key: 'signal', kind: 'line' }, { key: 'histogram', kind: 'histogram' }]` (add the histogram LAST so lines draw on top, RESEARCH Q3), `guides: () => [{ value: 0, color: '#8B92A0' }]` (static zero-line, D-46 — no Landmine #1 fix needed, 0 is never edited). In compute, build macd/signal via a toLineData-style helper and histogram via a toHistData helper that assigns each point one of 4 colors by comparing its value to zero and to the previous defined histogram value (positive rising `#22C55E` full / positive falling `#22C55E` 50% / negative more-negative `#EF4444` full / negative less-negative `#EF4444` 50%, UI-SPEC). Register `macd` in registry.ts. In store.ts addIndicator, seed distinct consecutive palette colors across a module's `kind:'line'` outputs ONLY when it has more than one line output AND no band output (derive from `module.outputs`); otherwise keep the current single-color behavior (leaves ma/rsi/bb unchanged, gives macd/signal two hues). Do NOT touch Chart.tsx.</action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run build</automated>
    <human-check>Launch `npm run dev`, add MACD via '+指標': it renders in its own pane with a zero line, a histogram in 4 distinct shades (deep/light green above zero, deep/light red below), and a MACD line + signal line in two different colors; move the crosshair — the MACD legend shows 'MACD .. Signal .. Hist ..' at 2 decimals updating in sync with the other panes. Backstop: load a 4+ integer-digit symbol and confirm the 3-value MACD readout does not wrap or clip in the legend row.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0 and `npm run build` exits 0.
    - `src/renderer/indicators/registry.ts` includes `macd`.
    - `src/renderer/indicators/macd.ts` declares 3 outputs (`macd`/`signal` lines + `histogram`), a static zero-line `guides`, and assigns per-bar histogram colors in compute.
    - `src/renderer/indicators/math.ts` `macd` computes the signal via a dense compacted array (no call of ema on the gappy macdLine).
    - `src/renderer/store.ts` gives a `macd` instance two different `colors.macd`/`colors.signal` values while `bb` remains single-color.
    - Chart.tsx is unmodified by this plan.
  </acceptance_criteria>
  <done>MACD renders in its own pane with correct EMA-signal math, a 4-color histogram, a zero line, and two distinguishable line hues, using Plan 01's machinery with no Chart.tsx change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MACD module unit test</name>
  <files>tests/indicators/macd.test.ts</files>
  <read_first>
    - tests/indicators/math.test.ts (Vitest conventions + comment-walkthrough house style)
    - src/renderer/indicators/macd.ts and src/renderer/indicators/math.ts (subjects), src/renderer/indicators/types.ts (HistPoint)
    - RESEARCH Q7 (alignment: macd first defined at slow−1, signal/histogram at slow−1+signal−1) and VALIDATION.md IND-06 row
  </read_first>
  <behavior>
    - `macd(values,12,26,9)` on ≥34-length input: macdLine first defined at index 25, signal + histogram first defined at index 33; all shorter indices undefined (alignment, Landmine #5).
    - The module compute returns exactly three keys `macd`/`signal`/`histogram`; histogram points carry a `color`; a rising positive histogram bar gets full-green, a falling one gets 50%-green (and mirror for negative/red).
    - histogram value ≈ macd − signal at aligned indices.
  </behavior>
  <action>Create tests/indicators/macd.test.ts mirroring math.test.ts conventions. Assert `macd()` output alignment (undefined before the computed first-defined indices, defined after), the compact/re-expand correctness (signal aligned one signal-window after macdLine), and the module compute's three output keys + per-bar 4-color histogram assignment (build a small crafted series that produces a known rising-positive and falling-positive bar). Do NOT assert TradingView golden values — that is Plan 04.</action>
  <verify>
    <automated>npx vitest run tests/indicators/macd.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/indicators/macd.test.ts` exits 0.
    - The test asserts macd/signal/histogram first-defined indices (25 / 33 / 33 for 12/26/9) and the 4-color histogram branch selection.
  </acceptance_criteria>
  <done>MACD's math alignment (including the gap-compaction), 3-output shape, and 4-color histogram logic are covered by a fast unit test that passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer UI numeric input → indicator param | User types MACD fast/slow/signal into the schema-driven edit form; value stays in-renderer (Zustand → client-side compute). No network/IPC/file/auth surface crossed (D-26). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02 | Tampering (V5 Input Validation) | MACD fast/slow/signal numeric params via IndicatorEditForm | low | mitigate | Reuse the existing Phase 3 number-field clamping (`Math.max(field.min, parsed)`); set FieldDesc `min: 1`/`step: 1` on fast/slow/signal. No new validation library. Params are numbers rendered as chart geometry, never reaching a DB/shell/HTML sink. |

No new trust boundary this phase (RESEARCH Security Domain). No package installs → no supply-chain (T-*-SC) threat. No high-severity threats → nothing blocks under `security_block_on: high`.
</threat_model>

<verification>
- `npm run typecheck` exits 0; `npm run build` exits 0; `npx vitest run tests/indicators/macd.test.ts` exits 0; `npm test` stays green.
- Manual UAT confirms the MACD pane, 4-color histogram, zero line, two-hue lines, and 3-value synchronized crosshair readout — plus the long-text backstop on a high-priced symbol — reusing Plan 01's machinery with no Chart.tsx change.
</verification>

<success_criteria>
- IND-06 (MACD in a dedicated pane: MACD line, signal, histogram, default 12/26/9) met with TradingView-convention EMA-signal math.
- The three sub-pane indicators (Volume/RSI/MACD) are all delivered; the correctness gate (Plan 04) can now assert all five indicators against golden values.
</success_criteria>

<output>
Create `.planning/phases/04-pane-indicators-crosshair-correct-math/04-03-SUMMARY.md` when done.
</output>
