---
phase: 04-pane-indicators-crosshair-correct-math
plan: 04
type: execute
wave: 4
depends_on: [04-01, 04-03]
files_modified:
  - tests/indicators/fixtures/golden.ts
  - tests/indicators/math.golden.test.ts
autonomous: true
requirements: [IND-09]
must_haves:
  truths:
    - "A single correctness-gate test (tests/indicators/math.golden.test.ts) covers all five indicators (SMA/EMA/BB/RSI/MACD) with the exact assertion Math.abs(actual − expected) < 0.01 (DD-2)."
    - "The golden fixture is self-contained: input OHLC and expected values both come from the same TradingView chart and the test never touches FMP, so a mismatch means our math is wrong, not a data-source/split difference (DD-1)."
    - "While expected values are placeholders the suite passes empty (it.todo/skip); when the user fills real TradingView numbers the same checkpoints begin enforcing — no test rewrite needed (D-49)."
    - "The fixture contains ≥34 bars before any MACD checkpoint (recommend 50-60 total) so MACD/signal/histogram are defined at every checkpoint index (RESEARCH Q7)."
    - "The gate re-confirms (does not rewrite) that bollinger() uses population standard deviation (÷period, not Bessel ÷period-1) and ema() is SMA-seeded, per IND-09/D-50 (RESEARCH Q7)."
  artifacts:
    - tests/indicators/fixtures/golden.ts
    - tests/indicators/math.golden.test.ts
  key_links:
    - "math.golden.test.ts imports sma/ema/bollinger from the existing math.ts plus rsi (Plan 01) and macd (Plan 03) — the single gate that proves TradingView parity across all v1 indicators (IND-09)."
  prohibitions:
    - "The golden fixture MUST be self-contained (no FMP fetch, no network import) — DD-1."
    - "Do NOT rewrite bollinger() or ema() — RESEARCH Q7 confirms they already satisfy IND-09; add a golden/structural assertion instead of touching math.ts."
    - "Use the explicit Math.abs(actual − expected) < 0.01 assertion form (DD-2), NOT toBeCloseTo — the placeholder→enforcing upgrade path (D-49) depends on this literal form being present."
    - "MACD checkpoints MUST sit at bar index ≥33 (for 12/26/9) so signal/histogram are defined there (Landmine #5 / RESEARCH Q7)."
---

<objective>
Land the IND-09 correctness gate: a self-contained, TradingView-sourced golden-fixture harness (DD-1) and a single cross-indicator test asserting all five v1 indicators (SMA/EMA/BB/RSI/MACD) match within `<0.01` (DD-2). The harness ships as a placeholder that passes empty (D-49) and flips to enforcing the moment the user drops in real TradingView values — with always-on structural assertions that immediately re-confirm the two "already correct" conventions (BB population stddev, EMA SMA-seed).

Purpose: make TradingView parity a permanent, mechanical gate rather than a one-time manual check — the highest-leverage protection against a silent math regression across all indicators.
Output: `fixtures/golden.ts` (≥50-bar placeholder AAPL-daily fixture + checkpoint list) and `math.golden.test.ts` (the enforcing harness), green on empty placeholders and ready for the user's TV numbers.
</objective>

<user_input_note>
The gate reaches full enforcement only when the user fills real TradingView values into `fixtures/golden.ts` (D-48/49). This plan delivers the harness in "empty-pass" state with `// TODO: fill from TradingView` markers and the exact assertion form pre-wired; no code change is needed later — the user replaces placeholder input OHLC + expected values (fixed symbol/range, AAPL daily working default) and the checkpoints self-enable. The always-on BB/EMA structural assertions enforce immediately regardless.
</user_input_note>

<artifacts_this_plan_produces>
- `tests/indicators/fixtures/golden.ts` — self-contained placeholder OHLC (`Bar[]`, ≥50 bars, TODO-marked) + a typed checkpoint list (`{ indicator, params, atIndex, expected }`, `expected` nullable for TODO) covering all 5 indicators
- `tests/indicators/math.golden.test.ts` — the cross-indicator gate: per-checkpoint `Math.abs(actual − expected) < 0.01` with `it.todo` for unfilled checkpoints, plus always-on BB-population-stddev and EMA-SMA-seed structural assertions
</artifacts_this_plan_produces>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/phases/04-pane-indicators-crosshair-correct-math/04-DESIGN.md
@.planning/phases/04-pane-indicators-crosshair-correct-math/04-RESEARCH.md
@.planning/phases/04-pane-indicators-crosshair-correct-math/04-PATTERNS.md
@.planning/phases/04-pane-indicators-crosshair-correct-math/04-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Self-contained golden fixture (placeholder)</name>
  <files>tests/indicators/fixtures/golden.ts</files>
  <read_first>
    - DESIGN §6 (golden-fixture harness), DD-1/2/3, D-48/49/50
    - RESEARCH Q7 (minimum fixture length: ≥34 bars before any MACD checkpoint for 12/26/9; recommend 50-60+) and PATTERNS.md `No Analog Found` row for golden.ts (shape pinned by DESIGN §6, no codebase analog)
    - src/shared/types.ts (`Bar` shape) and src/renderer/indicators/math.ts (function signatures the checkpoints will target)
  </read_first>
  <action>Create tests/indicators/fixtures/golden.ts as a self-contained module (DD-1 — no FMP/network import). Export `goldenBars: Bar[]` with ≥50 placeholder daily bars (plausible AAPL-daily-shaped OHLCV, clearly marked `// TODO: replace with real TradingView AAPL daily OHLC (fixed symbol/range, D-49)`), sized so MACD checkpoints can sit at index ≥33 (RESEARCH Q7). Export a typed checkpoint list `goldenCheckpoints` where each entry names an `indicator` ('sma'|'ema'|'bb'|'rsi'|'macd'), its `params` (e.g. period 14 for RSI; 12/26/9 for MACD; 20/2 for BB), the `atIndex` (bar index to read, with MACD entries at ≥33), which output `key` to read (e.g. 'line'/'upper'/'macd'/'signal'/'histogram'), and `expected: number | null` initialized to `null` with a `// TODO: fill from TradingView` comment. Include at least one checkpoint per indicator (all 5). No expected values are invented — placeholders stay null.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0.
    - `tests/indicators/fixtures/golden.ts` exports `goldenBars` with ≥50 bars and a `goldenCheckpoints` list containing at least one entry for each of sma/ema/bb/rsi/macd.
    - Every MACD checkpoint's `atIndex` is ≥33; every `expected` is `null` with a `TODO: fill from TradingView` marker; the file imports nothing from FMP/network.
  </acceptance_criteria>
  <done>A self-contained, correctly-sized placeholder golden fixture exists, covering all five indicators, ready for the user to paste real TradingView OHLC + expected values.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: All-five correctness-gate test (empty-pass, enforcing on fill)</name>
  <files>tests/indicators/math.golden.test.ts</files>
  <read_first>
    - tests/indicators/math.test.ts (Vitest import/describe/it style), tests/indicators/fixtures/golden.ts (Task 1 — goldenBars + goldenCheckpoints)
    - src/renderer/indicators/math.ts (sma/ema/bollinger + rsi from Plan 01 + macd from Plan 03) and src/renderer/indicators/types.ts
    - PATTERNS.md `math.golden.test.ts` section (tolerance form differs from math.test.ts: use `Math.abs(actual-expected)` `.toBeLessThan(0.01)`, NOT toBeCloseTo), DD-2, RESEARCH Q7 (BB population stddev + EMA SMA-seed re-confirmation)
  </read_first>
  <action>Create tests/indicators/math.golden.test.ts importing sma/ema/bollinger/rsi/macd from math.ts and goldenBars/goldenCheckpoints from the fixture. Iterate the checkpoints: for each, dispatch to the right math function over goldenBars' closes (or OHLC for BB source), read the value at `atIndex`/`key`, and when `expected !== null` assert `expect(Math.abs(actual - cp.expected)).toBeLessThan(0.01)` (DD-2); when `expected === null` register it as `it.todo(...)` so the suite passes empty until filled (D-49). Add two always-on structural assertions that enforce immediately (RESEARCH Q7): (a) bollinger() on a small known array matches the population-variance formula (÷period), not the sample formula (÷period-1); (b) ema()'s first defined value equals the SMA of the first `period` values (SMA-seed). Keep the comment-walkthrough house style from math.test.ts.</action>
  <verify>
    <automated>npx vitest run tests/indicators/math.golden.test.ts</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/indicators/math.golden.test.ts` exits 0 (empty-pass: unfilled checkpoints are `it.todo`, structural BB/EMA assertions pass) and `npm test` (full suite) stays green.
    - The file contains the literal `toBeLessThan(0.01)` assertion over `Math.abs(...)` and references all five of sma/ema/bollinger/rsi/macd.
    - Replacing a checkpoint's `expected: null` with a number causes that checkpoint to enforce (no code change needed) — confirm by temporarily setting one EMA/SMA checkpoint to its computed value and seeing the test run (then revert to null).
  </acceptance_criteria>
  <done>The correctness gate exists, passes on placeholders, enforces BB/EMA conventions immediately, and self-enables per-indicator the moment the user supplies TradingView values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Build/verification tooling only — Vitest runs in Node against a hardcoded self-contained fixture. No runtime tier, no user input, no network/IPC/file/auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| — | — | — | — | n/a | No applicable STRIDE threat: test-only code, no runtime execution path, no user input, no I/O, no package installs. No new trust boundary (RESEARCH Security Domain). |
</threat_model>

<verification>
- `npx vitest run tests/indicators/math.golden.test.ts` exits 0 on placeholders; `npm test` stays green.
- The gate's always-on structural assertions (BB population stddev, EMA SMA-seed) enforce immediately; the per-indicator TV checkpoints enforce as soon as the user fills real values (D-49) with no further code change.
</verification>

<success_criteria>
- IND-09 (indicator values match TradingView conventions: RSI Wilder, MACD EMA signal, BB population stddev, correct EMA seed — verified by a reference-value suite covering all five v1 indicators) has a landed, self-contained gate that goes from empty-pass to fully enforcing on user data entry.
</success_criteria>

<output>
Create `.planning/phases/04-pane-indicators-crosshair-correct-math/04-04-SUMMARY.md` when done.
</output>
